<div class="spacer">
    <h2>{{article.title}}
        <div class="controls">
            {{#if article.isEditable}}
            <a href="/article/edit/{{article.id}}">[edit]</a>
            {{/if}}
            {{#if user}}
            <a href="/article/history/{{article.id}}">[history]</a>
            {{/if}}
        </div>
    </h2>
    <article>
        {{#each article.paragraphs}}
          <p>{{this}}</p>
        {{/each}}
    </article>
</div>